{
    "scripts": {
        "build": "npm run build",
        "awsStart": "npm start",
        "start": "npm-run-all --parallel start-frontend start-backend",
        "start-frontend": "cd frontend && npm run dev",
        "start-backend": "cd backend && node app.ts"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5",
        "vite": "^4.5.0"
    },
    "dependencies": {
        "@aws-amplify/ui-react": "^5.3.2",
        "amplify": "^0.0.11",
        "aws-amplify": "^5.3.12",
        "aws-sdk": "^2.1490.0"
    }
}
